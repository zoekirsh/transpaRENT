import React from 'react';
import ListingCard from './ListingCard';

const ListingGrid = (allListings) => {
  return (
    <div>
      <h3>this will be the grid view of all listings</h3>
    </div>
  )
}

export default ListingGrid